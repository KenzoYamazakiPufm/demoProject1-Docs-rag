# ══════════════════════════════════════════════════════════════════════
# 白名单模式：默认忽略工作区里的**一切**，只放行 pkdb 项目自身的文件。
#
# 为什么反过来写：这个目录原本是个人杂物间，里面有
#   - .codebuddy/memory     含姓名、住址区域、求职意向、个人背景
#   - .codebuddy/plans      含简历结构与招聘调研记录
#   - 几十个招聘爬虫脚本与抓取数据（与 pkdb 无关）
# 这些都不该进仓库。黑名单容易漏（新增文件就漏了），白名单不会。
#
# 以后新增文件默认仍被忽略 —— 要进仓库必须在这里显式放行。
# ══════════════════════════════════════════════════════════════════════
/*

# ── 放行：pkdb 项目本体 ────────────────────────────────────────────────
!/.gitignore
!/README.md
!/TESTS.md
!/DEPLOY.md
!/requirements.txt
!/pkdb/
!/tests/
!/.streamlit/
!/.env.example

# ── 即使在放行目录里，这些也必须排除 ─────────────────────────────────
# 真实密钥：一旦提交，Key 会永久留在 git 历史里，删文件也抹不掉
.env
.streamlit/secrets.toml

# SQLite 运行期的临时文件（pkdb.sqlite3 本体**要提交**，云端靠它提供数据）
pkdb/data/*-journal
pkdb/data/*-wal
pkdb/data/*-shm

# Python / 测试缓存
__pycache__/
*.pyc
*.pyo
.pytest_cache/
